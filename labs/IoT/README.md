# IoT Lab
In this lab I configured multiple end devices to all be connected wirelessly to a networks default gateway. All devices have received IP addresses dynamically from the gateway and their power signal can be controlled fro
